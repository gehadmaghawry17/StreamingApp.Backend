@model IReadOnlyList<SeriesListItemViewModel>

@{
    ViewData["Title"] = "Series";
}

<section class="admin-page">
    <div class="admin-page-header">
        <div>
            <p class="admin-eyebrow">Content</p>
            <h1>Series</h1>
            <p class="admin-subtitle">Manage series records and their genre links.</p>
        </div>
        <a class="btn btn-primary" asp-action="Create">New Series</a>
    </div>

    @if (Model.Count == 0)
    {
        <div class="admin-empty">
            <h2>No series yet</h2>
            <p>Create a series after adding at least one genre.</p>
        </div>
    }
    else
    {
        <div class="admin-table-wrap">
            <table class="table admin-table align-middle">
                <thead>
                <tr>
                    <th>Title</th>
                    <th>Slug</th>
                    <th>Published</th>
                    <th>Genres</th>
                    <th class="text-end">Actions</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Title</td>
                        <td>@item.Slug</td>
                        <td>@(item.IsPublished ? "Yes" : "No")</td>
                        <td>@(string.IsNullOrWhiteSpace(item.Genres) ? "None" : item.Genres)</td>
                        <td class="admin-row-actions">
                            <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                            <form asp-action="Delete" asp-route-id="@item.Id" method="post" onsubmit="return confirm('Delete this series?');">
                                @Html.AntiForgeryToken()
                                <button class="btn btn-sm btn-outline-danger" type="submit">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    }
</section>
